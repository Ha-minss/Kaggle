# Original Colab Export (Reference Only)

This folder contains the original Colab-exported script (`untitled40.py`) for traceability.

- It includes Colab-specific lines like `!pip install ...` and `google.colab.files`.
- It is **not** meant to be the reproducible entrypoint.

Use the reproducible pipeline instead:
- `python -m scripts.run_experiment --train_csv data/train.csv ...`
